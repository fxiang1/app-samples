apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  annotations:
    include.release.openshift.io/ibm-cloud-managed: 'true'
    include.release.openshift.io/self-managed-high-availability: 'true'
    include.release.openshift.io/single-node-developer: 'true'
  name: cluster
spec:
  identityProviders:
    - htpasswd:
        fileData:
          name: app-e2e-users-test
      mappingMethod: claim
      name: app-e2e-htpasswd-test
      type: HTPasswd
    - htpasswd:
        fileData:
          name: htpasswd-d4pwb-test
      mappingMethod: claim
      name: feng-htpasswd-test
      type: HTPasswd

