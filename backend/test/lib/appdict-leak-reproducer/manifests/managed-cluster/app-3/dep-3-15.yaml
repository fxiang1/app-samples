apiVersion: apps/v1
kind: Deployment
metadata:
  name: dep-3-15
  labels:
    app: dep-3-15
    leak-mc-test: "true"
    app-group: app-3
  annotations:
    leak-test/churn-counter: "0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dep-3-15
  template:
    metadata:
      labels:
        app: dep-3-15
        leak-mc-test: "true"
    spec:
      containers:
        - name: worker
          image: registry.example.com/does-not-exist:v15
          command: ["sleep", "infinity"]
          resources:
            requests:
              memory: "8Mi"
              cpu: "1m"
            limits:
              memory: "32Mi"
              cpu: "10m"
