apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: dev-clusters
  namespace: open-cluster-management
spec:
  clusterLabels:
    matchLabels:
      environment: Dev
  clusterReplicas: 0
